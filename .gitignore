/target
/data
.env

# --- SECRETS / KEYS: never commit these ---
# Private keys. Committing this = anyone with the repo can drain the wallets.
wallets/
test-wallet.json
*keypair*.json
# Trade history + kill switch (operational state, not source).
sniper_audit.jsonl
/HALT

detected_pools.jsonl
*.jsonl
*.db
*.sqlite

# Curated smart-money wallet list. Not a secret in the key sense, but it is the
# research the alpha signal is built on — committing it publishes it.
tracked_wallets.json

# Claude Code local settings — machine-specific, and the harness writes
# transient .tmp files here that must never be committed.
.claude/
